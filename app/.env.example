# .env.example
# Copy to .env and fill in real values before running the app

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=realestate
DB_USER=postgres
DB_PASSWORD=your_password_here

# Optional convenience (keep in sync with the DB_* fields)
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/realestate
